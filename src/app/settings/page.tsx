import Settings from "@/components/settings";

export default function SettingsPage() {
  return (
    <div className="flex w-full flex-grow">
      <section className="flex w-full flex-grow px-4 sm:px-8">
        <Settings />
      </section>
    </div>
  );
}
